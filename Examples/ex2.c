/**
 * Suppose we now have multiple streets, each street has some houses,
 * and each house has multiple stories. 
 * How can we put the number 15 on a given floor of a given house on a given street?
 * Declare a 3D array and put number 10 in one of the locations
 * */

#include <stdio.h>

int main() {
    /**
     * Write your code below
     * */

    return 0;
}