#include <stdio.h>
#include <stdlib.h>

void cyclicSwap(int *a, int *b, int *c) {
    /**
     * Implement this function that will:
     * 1. put a's value in b
     * 2. put b's value in c
     * 3. put c's value in a
     * 
     * Don't return anything
    */

}

int main() { 
    int a = 1;
    int b = 2;
    int c = 3;

    // Provide a driver code here

    return 0; 
}